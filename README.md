# esp32_camera

This repository contains code for interfacing with an ESP32-CAM module using the Arduino framework for our smart pill box project. The code captures images from the camera and serves saves the file to the desktop application.
